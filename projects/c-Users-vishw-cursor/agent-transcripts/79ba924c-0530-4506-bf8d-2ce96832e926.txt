user:
<user_query>
build me a portfolio website for me
</user_query>

assistant:
[Thinking] **Planning file creation**

I need to use the Read tool before editing files, but here we will likely create new files via ApplyPatch. The system says I must use Read at least once, even if I want to create a new file, which is a bit confusing. However, it seems like I can just provide code snippets for the user instead of using the tools. Since the user is on Windows with PowerShell, I’ll give concise instructions on how to create and open files while covering the structure for a responsive single-page portfolio.
**Designing modern features**

I want to ensure a modern look for the user's project, which will involve using CSS with gradients, card layouts, hover effects, flexbox or grid, and media queries. For JavaScript, I'll include smooth scrolling and a mobile navigation toggle, and maybe even a theme toggle that's stored in localStorage. My goal is to keep the answer concise, but I expect the code will be substantial, and that’s totally fine. I'll prepare a solid response!
### Overview

Here’s a complete, modern, responsive single-page portfolio you can drop into a folder and open in your browser. It uses plain HTML/CSS/JS so you can host it anywhere (GitHub Pages, Netlify, etc.).

Below are three files:

- `index.html`
- `styles.css`
- `script.js`

You can replace placeholder text (name, bio, projects) with your own.

---

### `index.html`

```html
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Your Name – Portfolio</title>
  <meta name="description" content="Portfolio of Your Name – Software Developer." />
  <link rel="stylesheet" href="styles.css" />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap"
    rel="stylesheet"
  />
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <a href="#top" class="logo">
        <span class="logo-mark">Y</span>
        <span class="logo-text">Your Name</span>
      </a>

      <nav class="nav">
        <button class="nav-toggle" aria-label="Toggle navigation">
          <span></span><span></span><span></span>
        </button>
        <ul class="nav-links">
          <li><a href="#about">About</a></li>
          <li><a href="#skills">Skills</a></li>
          <li><a href="#projects">Projects</a></li>
          <li><a href="#experience">Experience</a></li>
          <li><a href="#contact">Contact</a></li>
          <li>
            <button class="theme-toggle" aria-label="Toggle dark mode">
              <span class="theme-icon theme-icon-moon">🌙</span>
              <span class="theme-icon theme-icon-sun">☀️</span>
            </button>
          </li>
        </ul>
      </nav>
    </div>
  </header>

  <main id="top">
    <!-- Hero -->
    <section class="section hero">
      <div class="container hero-grid">
        <div class="hero-text">
          <p class="eyebrow">Software Developer • Problem Solver</p>
          <h1>Hi, I'm <span class="accent">Your Name</span>.</h1>
          <p class="hero-subtitle">
            I build clean, scalable web applications and delightful user experiences.
          </p>
          <div class="hero-actions">
            <a href="#projects" class="btn primary">View my work</a>
            <a href="#contact" class="btn ghost">Contact me</a>
          </div>
          <div class="hero-meta">
            <span>📍 City, Country</span>
            <span>💼 Open to opportunities</span>
          </div>
        </div>

        <div class="hero-card">
          <div class="avatar">
            <!-- You can replace this with an <img> if you have a profile picture -->
            <span>Y</span>
          </div>
          <div class="hero-card-content">
            <h2>Full-Stack Developer</h2>
            <p>Specializing in JavaScript, React, and Node.js.</p>
            <ul class="hero-highlights">
              <li>3+ years building production web apps</li>
              <li>Strong focus on performance & accessibility</li>
              <li>Enjoys solving complex problems</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- About -->
    <section id="about" class="section">
      <div class="container section-inner">
        <header class="section-header">
          <h2>About</h2>
          <p>A quick introduction.</p>
        </header>
        <div class="grid-2">
          <div>
            <p>
              I'm a software developer with a passion for building products that are fast,
              accessible, and easy to use. I enjoy working across the stack, from crafting
              clean UI to designing robust backend systems.
            </p>
            <p>
              Recently, I've been focusing on modern frontend tooling, design systems, and
              scalable architectures for single-page applications.
            </p>
          </div>
          <div class="about-details">
            <div class="detail-row">
              <span class="label">Location</span><span>City, Country</span>
            </div>
            <div class="detail-row">
              <span class="label">Role</span><span>Software / Web Developer</span>
            </div>
            <div class="detail-row">
              <span class="label">Interests</span><span>Web dev, UX, open source</span>
            </div>
            <div class="detail-row">
              <span class="label">Email</span><a href="mailto:you@example.com">you@example.com</a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Skills -->
    <section id="skills" class="section section-alt">
      <div class="container section-inner">
        <header class="section-header">
          <h2>Skills</h2>
          <p>Technologies I work with.</p>
        </header>

        <div class="skills-grid">
          <div class="card">
            <h3>Frontend</h3>
            <ul class="tag-list">
              <li>HTML5</li>
              <li>CSS3 / SASS</li>
              <li>JavaScript (ES6+)</li>
              <li>TypeScript</li>
              <li>React</li>
              <li>Next.js</li>
            </ul>
          </div>
          <div class="card">
            <h3>Backend</h3>
            <ul class="tag-list">
              <li>Node.js</li>
              <li>Express</li>
              <li>REST APIs</li>
              <li>PostgreSQL</li>
              <li>MongoDB</li>
            </ul>
          </div>
          <div class="card">
            <h3>Tools & Practices</h3>
            <ul class="tag-list">
              <li>Git & GitHub</li>
              <li>Docker</li>
              <li>CI/CD</li>
              <li>Unit Testing</li>
              <li>Agile</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- Projects -->
    <section id="projects" class="section">
      <div class="container section-inner">
        <header class="section-header">
          <h2>Projects</h2>
          <p>A selection of things I've built.</p>
        </header>

        <div class="projects-grid">
          <article class="card project-card">
            <div class="project-meta">
              <h3>Project One</h3>
              <p class="project-tagline">A dashboard for tracking personal productivity.</p>
            </div>
            <p>
              Built with React, TypeScript, and a Node.js backend. Features real-time charts,
              authentication, and responsive design.
            </p>
            <ul class="tag-list">
              <li>React</li>
              <li>TypeScript</li>
              <li>Node.js</li>
            </ul>
            <div class="project-links">
              <a href="#" target="_blank" rel="noreferrer">Live Demo</a>
              <a href="#" target="_blank" rel="noreferrer">GitHub</a>
            </div>
          </article>

          <article class="card project-card">
            <div class="project-meta">
              <h3>Project Two</h3>
              <p class="project-tagline">An API for managing tasks and workflows.</p>
            </div>
            <p>
              RESTful API with authentication, role-based access, and documentation using
              OpenAPI/Swagger.
            </p>
            <ul class="tag-list">
              <li>Node.js</li>
              <li>Express</li>
              <li>PostgreSQL</li>
            </ul>
            <div class="project-links">
              <a href="#" target="_blank" rel="noreferrer">GitHub</a>
            </div>
          </article>

          <article class="card project-card">
            <div class="project-meta">
              <h3>Project Three</h3>
              <p class="project-tagline">A minimal design system for web apps.</p>
            </div>
            <p>
              A reusable set of components built with accessible HTML and modern CSS, focusing
              on typography, spacing, and theming.
            </p>
            <ul class="tag-list">
              <li>Design Systems</li>
              <li>CSS</li>
              <li>Accessibility</li>
            </ul>
            <div class="project-links">
              <a href="#" target="_blank" rel="noreferrer">GitHub</a>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- Experience -->
    <section id="experience" class="section section-alt">
      <div class="container section-inner">
        <header class="section-header">
          <h2>Experience</h2>
          <p>Where I've worked and what I've done.</p>
        </header>

        <div class="timeline">
          <article class="timeline-item">
            <div class="timeline-dot"></div>
            <div class="timeline-content">
              <div class="timeline-header">
                <h3>Job Title – Company</h3>
                <span class="timeline-date">2023 – Present</span>
              </div>
              <p>
                Short description of your role and impact. Mention key technologies,
                responsibilities, and achievements.
              </p>
              <ul>
                <li>Improved performance of key features by X%.</li>
                <li>Collaborated with designers and PMs to deliver features on time.</li>
              </ul>
            </div>
          </article>

          <article class="timeline-item">
            <div class="timeline-dot"></div>
            <div class="timeline-content">
              <div class="timeline-header">
                <h3>Previous Role – Company</h3>
                <span class="timeline-date">2021 – 2023</span>
              </div>
              <p>
                Another brief summary focused on impact. Highlight technologies and
                accomplishments.
              </p>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- Contact -->
    <section id="contact" class="section">
      <div class="container section-inner">
        <header class="section-header">
          <h2>Contact</h2>
          <p>Let's work together.</p>
        </header>

        <div class="grid-2 contact-grid">
          <div>
            <p>
              If you’d like to discuss a project, collaboration, or just say hi, feel free to
              reach out. I try to respond within a day or two.
            </p>
            <ul class="contact-list">
              <li><span>Email</span> <a href="mailto:you@example.com">you@example.com</a></li>
              <li><span>GitHub</span> <a href="https://github.com/yourusername" target="_blank" rel="noreferrer">github.com/yourusername</a></li>
              <li><span>LinkedIn</span> <a href="https://linkedin.com/in/yourusername" target="_blank" rel="noreferrer">linkedin.com/in/yourusername</a></li>
            </ul>
          </div>
          <form class="contact-form" action="https://formspree.io/f/your-id" method="POST">
            <div class="field">
              <label for="name">Name</label>
              <input id="name" name="name" type="text" required />
            </div>
            <div class="field">
              <label for="email">Email</label>
              <input id="email" name="_replyto" type="email" required />
            </div>
            <div class="field">
              <label for="message">Message</label>
              <textarea id="message" name="message" rows="4" required></textarea>
            </div>
            <button type="submit" class="btn primary full-width">Send message</button>
            <p class="form-note">
              This form uses Formspree. Replace the `action` URL with your own endpoint.
            </p>
          </form>
        </div>
      </div>
    </section>
  </main>

  <footer class="site-footer">
    <div class="container footer-inner">
      <span>© <span id="year"></span> Your Name</span>
      <span>Built with ❤️ and JavaScript</span>
    </div>
  </footer>

  <script src="script.js"></script>
</body>
</html>
```

---

### `styles.css`

```css
:root {
  --bg: #050816;
  --bg-elevated: #0b1020;
  --bg-elevated-soft: #111827;
  --bg-alt: #050816;
  --border-subtle: rgba(148, 163, 184, 0.3);
  --text: #e5e7eb;
  --text-muted: #9ca3af;
  --accent: #6366f1;
  --accent-soft: rgba(99, 102, 241, 0.2);
  --accent-strong: #4f46e5;
  --danger: #ef4444;
  --radius-lg: 16px;
  --radius-md: 10px;
  --radius-full: 999px;
  --shadow-soft: 0 18px 60px rgba(15, 23, 42, 0.75);
  --shadow-subtle: 0 10px 40px rgba(15, 23, 42, 0.6);
  --border-width: 1px;
}

:root[data-theme='light'] {
  --bg: #f3f4f6;
  --bg-elevated: #ffffff;
  --bg-elevated-soft: #f9fafb;
  --bg-alt: #e5e7eb;
  --border-subtle: rgba(148, 163, 184, 0.4);
  --text: #111827;
  --text-muted: #6b7280;
  --accent: #4f46e5;
  --accent-soft: rgba(79, 70, 229, 0.08);
  --accent-strong: #4338ca;
  --shadow-soft: 0 18px 40px rgba(15, 23, 42, 0.18);
  --shadow-subtle: 0 10px 30px rgba(15, 23, 42, 0.12);
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  padding: 0;
}

body {
  font-family: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI',
    sans-serif;
  background: radial-gradient(circle at top, #111827 0, #020617 38%, #020617 100%);
  color: var(--text);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

img {
  max-width: 100%;
  display: block;
}

a {
  color: inherit;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

/* Layout */

.container {
  width: min(1120px, 100% - 2rem);
  margin-inline: auto;
}

.section {
  padding: 5.5rem 0;
}

.section-alt {
  background: radial-gradient(circle at top left, #111827 0, #020617 45%);
}

.section-inner {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.section-header h2 {
  margin: 0;
  font-size: 2rem;
  letter-spacing: 0.03em;
}

.section-header p {
  margin-top: 0.25rem;
  color: var(--text-muted);
}

.grid-2 {
  display: grid;
  grid-template-columns: minmax(0, 1.5fr) minmax(0, 1fr);
  gap: 2.5rem;
}

@media (max-width: 768px) {
  .section {
    padding: 3.75rem 0;
  }

  .grid-2 {
    grid-template-columns: minmax(0, 1fr);
  }
}

/* Header / Nav */

.site-header {
  position: sticky;
  top: 0;
  z-index: 40;
  backdrop-filter: blur(18px);
  background: linear-gradient(
    to bottom,
    rgba(2, 6, 23, 0.9),
    rgba(2, 6, 23, 0.72),
    transparent
  );
  border-bottom: 1px solid rgba(15, 23, 42, 0.8);
}

.header-inner {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-block: 0.8rem;
}

.logo {
  display: inline-flex;
  align-items: center;
  gap: 0.6rem;
  font-weight: 600;
  letter-spacing: 0.06em;
  text-transform: uppercase;
}

.logo-mark {
  width: 32px;
  height: 32px;
  border-radius: 12px;
  background: radial-gradient(circle at 25% 0, #a78bfa, #4f46e5, #0ea5e9);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 1rem;
  box-shadow: 0 12px 28px rgba(79, 70, 229, 0.7);
}

.logo-text {
  font-size: 0.95rem;
}

.nav {
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  list-style: none;
  padding: 0;
  margin: 0;
}

.nav-links a {
  font-size: 0.9rem;
  color: var(--text-muted);
  padding: 0.35rem 0;
  border-bottom: 1px solid transparent;
  transition: color 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
}

.nav-links a:hover {
  color: var(--text);
  border-color: rgba(99, 102, 241, 0.6);
  transform: translateY(-1px);
}

/* Nav toggle (mobile) */

.nav-toggle {
  display: none;
  flex-direction: column;
  gap: 0.18rem;
  width: 32px;
  height: 32px;
  border-radius: 999px;
  border: none;
  background: rgba(15, 23, 42, 0.85);
  justify-content: center;
  align-items: center;
  cursor: pointer;
  padding: 0;
}

.nav-toggle span {
  display: block;
  width: 15px;
  height: 1.75px;
  border-radius: 999px;
  background: #e5e7eb;
  transition: transform 0.18s ease, opacity 0.18s ease;
}

.nav-open .nav-toggle span:nth-child(1) {
  transform: translateY(4px) rotate(45deg);
}
.nav-open .nav-toggle span:nth-child(2) {
  opacity: 0;
}
.nav-open .nav-toggle span:nth-child(3) {
  transform: translateY(-4px) rotate(-45deg);
}

@media (max-width: 768px) {
  .nav-toggle {
    display: flex;
  }

  .nav-links {
    position: fixed;
    inset-inline: 0;
    top: 56px;
    padding: 1.2rem 1.2rem 1.6rem;
    background: rgba(2, 6, 23, 0.98);
    border-bottom: 1px solid rgba(15, 23, 42, 0.95);
    flex-direction: column;
    gap: 0.85rem;
    transform: translateY(-120%);
    opacity: 0;
    pointer-events: none;
    transition: transform 0.22s ease, opacity 0.22s ease;
  }

  .nav-open .nav-links {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }
}

/* Buttons */

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.35rem;
  padding: 0.72rem 1.3rem;
  border-radius: var(--radius-full);
  border: var(--border-width) solid transparent;
  font-size: 0.9rem;
  font-weight: 500;
  cursor: pointer;
  transition: transform 0.16s ease, box-shadow 0.16s ease, background 0.16s ease,
    border-color 0.16s ease, color 0.16s ease;
  text-decoration: none;
}

.btn.primary {
  background: radial-gradient(circle at 20% 0, #818cf8, #4f46e5, #0ea5e9);
  color: white;
  box-shadow: 0 18px 40px rgba(79, 70, 229, 0.9);
}

.btn.primary:hover {
  transform: translateY(-1px);
  box-shadow: 0 20px 46px rgba(79, 70, 229, 1);
}

.btn.ghost {
  border-color: rgba(148, 163, 184, 0.5);
  background: rgba(15, 23, 42, 0.65);
  color: var(--text);
}

.btn.ghost:hover {
  background: rgba(15, 23, 42, 0.9);
}

.btn.full-width {
  width: 100%;
}

/* Hero */

.hero {
  padding-top: 4.25rem;
}

.hero-grid {
  display: grid;
  grid-template-columns: minmax(0, 1.6fr) minmax(0, 1.1fr);
  gap: 2.8rem;
  align-items: center;
}

.eyebrow {
  font-size: 0.8rem;
  letter-spacing: 0.22em;
  text-transform: uppercase;
  color: var(--accent);
  margin-bottom: 0.7rem;
}

.hero-text h1 {
  font-size: clamp(2.4rem, 3vw, 2.9rem);
  line-height: 1.08;
  margin: 0 0 1rem;
}

.hero-text .accent {
  background: linear-gradient(to right, #a78bfa, #4f46e5, #22d3ee);
  -webkit-background-clip: text;
  color: transparent;
}

.hero-subtitle {
  color: var(--text-muted);
  font-size: 1rem;
  max-width: 32rem;
  margin-bottom: 1.3rem;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 0.9rem;
  margin-bottom: 0.9rem;
}

.hero-meta {
  display: flex;
  gap: 1.6rem;
  font-size: 0.85rem;
  color: var(--text-muted);
}

.hero-card {
  border-radius: 26px;
  padding: 1.6rem 1.6rem 1.8rem;
  border: 1px solid rgba(148, 163, 184, 0.35);
  background:
    radial-gradient(circle at 0 0, rgba(56, 189, 248, 0.1), transparent 60%),
    radial-gradient(circle at 100% 0, rgba(129, 140, 248, 0.12), transparent 60%),
    linear-gradient(145deg, #020617, #020617);
  box-shadow: var(--shadow-soft);
  display: flex;
  flex-direction: column;
  gap: 1.1rem;
}

.avatar {
  width: 64px;
  height: 64px;
  border-radius: 22px;
  background: radial-gradient(circle at 20% 0, #a855f7, #4f46e5, #0ea5e9);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 1.6rem;
  color: white;
  box-shadow: 0 18px 45px rgba(79, 70, 229, 0.9);
}

.hero-card-content h2 {
  margin: 0;
  font-size: 1.1rem;
}

.hero-card-content p {
  margin: 0.35rem 0 0.7rem;
  color: var(--text-muted);
  font-size: 0.93rem;
}

.hero-highlights {
  list-style: none;
  padding: 0;
  margin: 0;
  display: grid;
  gap: 0.3rem;
  font-size: 0.88rem;
  color: var(--text-muted);
}

.hero-highlights li::before {
  content: '•';
  color: var(--accent);
  margin-right: 0.45rem;
}

@media (max-width: 900px) {
  .hero-grid {
    grid-template-columns: minmax(0, 1fr);
  }

  .hero-card {
    order: -1;
  }

  .hero {
    padding-top: 3.5rem;
  }
}

@media (max-width: 640px) {
  .hero-meta {
    flex-direction: column;
    gap: 0.35rem;
  }
}

/* About */

.about-details {
  border-radius: var(--radius-lg);
  padding: 1.4rem 1.3rem;
  background: radial-gradient(circle at top left, #111827, #020617);
  border: 1px solid rgba(148, 163, 184, 0.35);
  box-shadow: var(--shadow-subtle);
  display: flex;
  flex-direction: column;
  gap: 0.7rem;
  font-size: 0.92rem;
}

.detail-row {
  display: flex;
  justify-content: space-between;
  gap: 0.9rem;
}

.detail-row .label {
  color: var(--text-muted);
}

/* Skills */

.skills-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1.4rem;
}

.card {
  border-radius: var(--radius-lg);
  padding: 1.4rem 1.3rem;
  background: radial-gradient(circle at 0 0, #111827, #020617 60%);
  border: 1px solid rgba(148, 163, 184, 0.35);
  box-shadow: var(--shadow-subtle);
}

.card h3 {
  margin-top: 0;
  margin-bottom: 0.5rem;
  font-size: 1rem;
}

.tag-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.4rem;
}

.tag-list li {
  font-size: 0.8rem;
  padding: 0.22rem 0.6rem;
  border-radius: 999px;
  background: rgba(15, 23, 42, 0.85);
  border: 1px solid rgba(148, 163, 184, 0.5);
}

:root[data-theme='light'] .tag-list li {
  background: rgba(243, 244, 246, 0.95);
}

@media (max-width: 900px) {
  .skills-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 640px) {
  .skills-grid {
    grid-template-columns: minmax(0, 1fr);
  }
}

/* Projects */

.projects-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 1.4rem;
}

.project-card {
  position: relative;
  overflow: hidden;
}

.project-card::before {
  content: '';
  position: absolute;
  inset: -40%;
  background: radial-gradient(circle at var(--mouse-x, 50%) var(--mouse-y, 0),
      rgba(94, 234, 212, 0.15),
      transparent 55%);
  opacity: 0;
  transition: opacity 0.25s ease;
  pointer-events: none;
}

.project-card:hover::before {
  opacity: 1;
}

.project-meta {
  display: flex;
  flex-direction: column;
  gap: 0.1rem;
  margin-bottom: 0.4rem;
}

.project-meta h3 {
  margin: 0;
  font-size: 1.05rem;
}

.project-tagline {
  margin: 0;
  font-size: 0.9rem;
  color: var(--text-muted);
}

.project-card p {
  font-size: 0.9rem;
  color: var(--text-muted);
}

.project-links {
  margin-top: 0.9rem;
  display: flex;
  flex-wrap: wrap;
  gap: 0.7rem;
  font-size: 0.88rem;
}

.project-links a {
  color: var(--accent);
  text-decoration: none;
}

.project-links a:hover {
  text-decoration: underline;
}

@media (max-width: 900px) {
  .projects-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 640px) {
  .projects-grid {
    grid-template-columns: minmax(0, 1fr);
  }
}

/* Timeline */

.timeline {
  position: relative;
  padding-left: 1.6rem;
  border-left: 1px solid rgba(148, 163, 184, 0.45);
  display: grid;
  gap: 1.6rem;
}

.timeline-item {
  position: relative;
}

.timeline-dot {
  position: absolute;
  left: -0.68rem;
  top: 0.45rem;
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--accent);
  box-shadow: 0 0 0 4px rgba(99, 102, 241, 0.28);
}

.timeline-content {
  border-radius: var(--radius-md);
  padding: 1.05rem 1.1rem;
  background: rgba(15, 23, 42, 0.84);
  border: 1px solid rgba(148, 163, 184, 0.4);
}

:root[data-theme='light'] .timeline-content {
  background: rgba(255, 255, 255, 0.96);
}

.timeline-header {
  display: flex;
  justify-content: space-between;
  gap: 1rem;
  align-items: baseline;
}

.timeline-header h3 {
  margin: 0;
  font-size: 0.98rem;
}

.timeline-date {
  font-size: 0.8rem;
  color: var(--text-muted);
}

.timeline-content p {
  margin: 0.45rem 0;
  font-size: 0.9rem;
  color: var(--text-muted);
}

.timeline-content ul {
  padding-left: 1.1rem;
  margin: 0.2rem 0 0;
  font-size: 0.9rem;
  color: var(--text-muted);
}

/* Contact */

.contact-grid {
  align-items: flex-start;
}

.contact-list {
  list-style: none;
  padding: 0;
  margin: 1rem 0 0;
  display: grid;
  gap: 0.4rem;
  font-size: 0.9rem;
}

.contact-list li {
  display: flex;
  gap: 0.7rem;
}

.contact-list span {
  min-width: 64px;
  color: var(--text-muted);
}

.contact-form {
  border-radius: var(--radius-lg);
  padding: 1.5rem 1.4rem;
  background: radial-gradient(circle at 0 0, #111827, #020617 65%);
  border: 1px solid rgba(148, 163, 184, 0.35);
  box-shadow: var(--shadow-subtle);
  display: grid;
  gap: 0.9rem;
}

.field {
  display: grid;
  gap: 0.25rem;
}

label {
  font-size: 0.84rem;
  color: var(--text-muted);
}

input,
textarea {
  border-radius: 10px;
  border: 1px solid rgba(148, 163, 184, 0.6);
  padding: 0.58rem 0.7rem;
  font: inherit;
  background: rgba(15, 23, 42, 0.92);
  color: var(--text);
  outline: none;
  transition: border-color 0.18s ease, box-shadow 0.18s ease, background 0.18s ease;
}

:root[data-theme='light'] input,
:root[data-theme='light'] textarea {
  background: rgba(249, 250, 251, 0.96);
}

input:focus,
textarea:focus {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px rgba(79, 70, 229, 0.7);
}

textarea {
  resize: vertical;
}

.form-note {
  margin: 0;
  font-size: 0.78rem;
  color: var(--text-muted);
}

/* Footer */

.site-footer {
  border-top: 1px solid rgba(15, 23, 42, 0.7);
  padding: 1.4rem 0 1.6rem;
  background: radial-gradient(circle at top, #020617 0, #020617 100%);
}

.footer-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 1rem;
  font-size: 0.82rem;
  color: var(--text-muted);
}

@media (max-width: 640px) {
  .hero-grid,
  .section-inner {
    gap: 2rem;
  }

  .footer-inner {
    flex-direction: column;
    align-items: flex-start;
  }
}

/* Theme toggle */

.theme-toggle {
  width: 34px;
  height: 34px;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.7);
  background: rgba(15, 23, 42, 0.9);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  padding: 0;
  position: relative;
  overflow: hidden;
}

.theme-icon {
  position: absolute;
  font-size: 0.9rem;
  transition: transform 0.2s ease, opacity 0.2s ease;
}

.theme-icon-sun {
  transform: translateY(16px);
  opacity: 0;
}

[data-theme='light'] .theme-icon-sun {
  transform: translateY(0);
  opacity: 1;
}

[data-theme='light'] .theme-icon-moon {
  transform: translateY(-16px);
  opacity: 0;
}
```

---

### `script.js`

```javascript
// Smooth scroll for nav links
document.querySelectorAll('a[href^="#"]').forEach((link) => {
  link.addEventListener('click', (e) => {
    const targetId = link.getAttribute('href');
    if (!targetId || targetId === '#') return;
    const targetEl = document.querySelector(targetId);
    if (!targetEl) return;

    e.preventDefault();
    const yOffset = -72;
    const y =
      targetEl.getBoundingClientRect().top + window.pageYOffset + yOffset;

    window.scrollTo({ top: y, behavior: 'smooth' });

    document.body.classList.remove('nav-open');
  });
});

// Mobile nav toggle
const navToggle = document.querySelector('.nav-toggle');
if (navToggle) {
  navToggle.addEventListener('click', () => {
    document.body.classList.toggle('nav-open');
  });
}

// Simple dark/light theme toggle with localStorage
const root = document.documentElement;
const storedTheme = localStorage.getItem('theme');
if (storedTheme === 'light' || storedTheme === 'dark') {
  root.setAttribute('data-theme', storedTheme);
}

const themeToggle = document.querySelector('.theme-toggle');
if (themeToggle) {
  themeToggle.addEventListener('click', () => {
    const current = root.getAttribute('data-theme') || 'dark';
    const next = current === 'dark' ? 'light' : 'dark';
    root.setAttribute('data-theme', next);
    localStorage.setItem('theme', next);
  });
}

// Set current year in footer
const yearSpan = document.getElementById('year');
if (yearSpan) {
  yearSpan.textContent = new Date().getFullYear();
}

// Subtle project hover spotlight (handles)
document.querySelectorAll('.project-card').forEach((card) => {
  card.addEventListener('mousemove', (e) => {
    const rect = card.getBoundingClientRect();
    const x = ((e.clientX - rect.left) / rect.width) * 100;
    const y = ((e.clientY - rect.top) / rect.height) * 100;
    card.style.setProperty('--mouse-x', `${x}%`);
    card.style.setProperty('--mouse-y', `${y}%`);
  });
});
```

---

### How to run it locally

1. **Create a folder** on your machine, e.g. `C:\Users\vishw\portfolio`.
2. Inside it, create three files named **exactly**: `index.html`, `styles.css`, `script.js`.
3. Paste the corresponding code into each file and save.
4. Double-click `index.html` (or right-click → Open With → your browser).

If you tell me your **name**, **location**, **years of experience**, and **3–5 projects** (titles + 1–2 line descriptions + tech stack), I can customize this template exactly for you.