# PuppyBro - Environment Variables
# Copy to .env and set values. Never commit .env to version control.

PORT=3000

# Required: JWT signing secret. Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Rotate periodically in production.
JWT_SECRET=your-secure-secret-min-32-chars-change-in-production
